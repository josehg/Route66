{% extends 'base.html.twig' %}
{% block body %}
<div class="container">
    <!-- Example row of columns -->
    <div class="row">
        <div class="col-md-12">
            <h1>Bienvenido {{ app.user.nombre }} {{ app.user.apellidos }}!</h1>
            <p>Panel de administración</p>
            <ul>
                <li><a href="{{ path('listado_usuarios') }}">Usuarios</a></li>
                <li><a href="{{ path('listado_categorias') }}">Categorías</a></li>
                <li><a href="{{ path('listado_negocios') }}">Negocios</a></li>
            </ul>
        </div>
    </div>
</div>
{% endblock %}
